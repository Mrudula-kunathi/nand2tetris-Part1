/**
 * AND gate: out = a AND b
 */

CHIP And {
    IN a, b;    // Input pins
    OUT out;    // Output pin

    PARTS:
    Nand(a=a, b=b, out=nandOut);  // Using NAND gate to implement AND gate
    Not(in=nandOut, out=out);     // Inverting the output of the NAND gate to get AND
}
